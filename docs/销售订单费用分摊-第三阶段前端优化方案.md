# 销售订单费用分摊 - 第三阶段前端优化方案

> 基于2025年React最佳实践的前端组件重构方案
>
> **文档日期**: 2025-01-11
> **优先级**: P1 (重要优化)
> **工作量估算**: 2-3天
> **参考**: 销售订单费用分摊方案设计、React Hook Form最佳实践2025

---

## 📊 执行摘要

### 当前实现分析

**现有组件**: `components/sales-orders/fee-items-input.tsx`

**✅ 做得好的地方**:

1. 使用 `React.memo` 优化性能
2. 数据清洗函数 `sanitizeFeeItems` 确保数据一致性
3. 受控组件模式保持数据同步
4. 费用类型变更时自动更新默认值

**❌ 存在的问题**:

1. **未使用 React Hook Form**: 手动管理表单状态,代码复杂度高
2. **缺少 Zod 验证**: 无实时验证和错误提示
3. **缺少"承担方"字段**: 第三阶段核心需求未实现
4. **可访问性不足**: 缺少 ARIA 属性和键盘导航支持
5. **错误处理缺失**: 无验证错误提示机制
6. **组件职责混乱**: 同时负责状态管理、UI渲染和业务逻辑

### 优化目标

**核心目标**:

1. **集成 React Hook Form + Zod**: 使用业界标准方案管理动态表单
2. **添加"承担方"字段**: 完成第三阶段核心功能
3. **提升可访问性**: 符合 WCAG 2.1 AA 标准
4. **改善错误处理**: 实时验证和友好的错误提示
5. **优化组件架构**: 使用复合组件模式 (Compound Components)

---

## 🎯 2025年最佳实践对照

### 1. React Hook Form + Zod 集成

#### 当前实现 (不推荐)

```typescript
// ❌ 手动管理状态,代码冗余
const [localItems, setLocalItems] = useState<SalesOrderFeeItem[]>([]);

const handleUpdateFeeItem = (index: number, field: string, value: any) => {
  const updated = localItems.map((item, i) => {
    if (i === index) {
      return { ...item, [field]: value };
    }
    return item;
  });
  emitChange(updated);
};
```

#### 最佳实践 (2025)

```typescript
// ✅ 使用 useFieldArray 管理动态数组
import { useFieldArray } from 'react-hook-form';

const { fields, append, remove, update } = useFieldArray({
  control,
  name: 'feeItems',
});

// 自动处理状态更新、验证和错误追踪
```

**收益**:

- 减少 60% 的状态管理代码
- 自动验证和错误处理
- 更好的性能 (避免不必要的重渲染)

### 2. Zod Schema 验证

#### 最佳实践 (2025)

```typescript
// ✅ 声明式验证规则
const feeItemSchema = z.object({
  feeType: z.enum(['freight', 'processing', 'packaging', 'other']),
  feeName: z.string().min(1, '费用名称不能为空'),
  feeAmount: z.number().nonnegative('费用金额不能为负'),
  paidBy: z.enum(['customer', 'company']),
  remarks: z.string().optional(),
});

// 数组验证
const feeItemsSchema = z.array(feeItemSchema).min(0);
```

**收益**:

- 类型安全 + 运行时验证
- 声明式验证规则
- 自动生成 TypeScript 类型

### 3. 复合组件模式 (Compound Components)

#### 当前实现 (不推荐)

```typescript
// ❌ 单体组件,职责混乱
export const FeeItemsInput = ({ feeItems, onChange }) => {
  // 200+ 行代码混合在一起
  return <div>...</div>;
};
```

#### 最佳实践 (2025)

```typescript
// ✅ 复合组件,职责清晰
export const FeeItemsInput = {
  Root: FeeItemsRoot,
  List: FeeItemsList,
  Item: FeeItem,
  AddButton: AddFeeItemButton,
  Summary: FeeItemsSummary,
};

// 使用方式
<FeeItemsInput.Root>
  <FeeItemsInput.AddButton />
  <FeeItemsInput.List>
    {fields.map((field, index) => (
      <FeeItemsInput.Item key={field.id} index={index} />
    ))}
  </FeeItemsInput.List>
  <FeeItemsInput.Summary />
</FeeItemsInput.Root>
```

**收益**:

- 单一职责原则 (SRP)
- 易于测试和维护
- 更好的可组合性

### 4. 可访问性 (a11y)

#### 当前实现 (不足)

```typescript
// ❌ 缺少 ARIA 属性和语义化标签
<div className="col-span-3">
  <Label>费用类型</Label>
  <Select>...</Select>
</div>
```

#### 最佳实践 (2025)

```typescript
// ✅ 完整的可访问性支持
<FormField
  control={control}
  name={`feeItems.${index}.feeType`}
  render={({ field }) => (
    <FormItem>
      <FormLabel htmlFor={field.name}>费用类型</FormLabel>
      <Select
        {...field}
        aria-label="选择费用类型"
        aria-describedby={`${field.name}-description`}
      >
        ...
      </Select>
      <FormDescription id={`${field.name}-description`}>
        选择本次费用的类型
      </FormDescription>
      <FormMessage /> {/* 自动显示验证错误 */}
    </FormItem>
  )}
/>
```

**收益**:

- 符合 WCAG 2.1 AA 标准
- 屏幕阅读器友好
- 键盘导航支持

---

## 🏗️ 优化架构设计

### 组件层次结构

```
FeeItemsFormField (容器组件)
├─ FeeItemsProvider (Context: 共享状态)
│  └─ feeItemsContext: { fields, append, remove, totalFees, errors }
│
├─ FeeItemsHeader
│  ├─ 标题 + 计数
│  └─ AddFeeItemButton
│
├─ FeeItemsList
│  └─ FeeItemCard (可重复)
│     ├─ FeeTypeField (费用类型选择器)
│     ├─ FeeNameField (费用名称输入)
│     ├─ FeeAmountField (费用金额输入)
│     ├─ FeePaidByField (承担方选择器) ⭐ 新增
│     ├─ FeeRemarksField (备注输入)
│     └─ RemoveFeeButton
│
└─ FeeItemsSummary
   ├─ 客户承担费用小计
   ├─ 公司承担费用小计
   └─ 费用总计
```

### 数据流设计

```typescript
// 1. 父组件 (销售订单表单)
<Form {...form}>
  <FeeItemsFormField control={form.control} />
</Form>

// 2. FeeItemsFormField (React Hook Form 集成)
const { fields, append, remove } = useFieldArray({
  control,
  name: 'feeItems',
});

// 3. FeeItemsProvider (状态共享)
<FeeItemsContext.Provider value={{ fields, append, remove, ... }}>
  <FeeItemsHeader />
  <FeeItemsList />
  <FeeItemsSummary />
</FeeItemsContext.Provider>

// 4. 子组件通过 Context 访问共享状态
const { append } = useFeeItemsContext();
```

---

## 💻 核心代码实现

### 1. Zod Schema 定义

```typescript
// lib/validations/sales-order-fee.ts

import { z } from 'zod';

/**
 * 销售订单费用项验证 Schema
 */
export const salesOrderFeeItemSchema = z.object({
  id: z.string().optional(),
  feeType: z.enum(
    ['freight', 'processing', 'packaging', 'loading_unloading', 'other'],
    {
      required_error: '请选择费用类型',
      invalid_type_error: '费用类型无效',
    }
  ),
  feeName: z
    .string()
    .min(1, '费用名称不能为空')
    .max(100, '费用名称不能超过100个字符'),
  feeAmount: z
    .number({
      required_error: '请输入费用金额',
      invalid_type_error: '费用金额必须是数字',
    })
    .nonnegative('费用金额不能为负数')
    .finite('费用金额必须是有限数字'),
  paidBy: z.enum(['customer', 'company'], {
    required_error: '请选择费用承担方',
    invalid_type_error: '费用承担方无效',
  }),
  remarks: z.string().max(500, '备注不能超过500个字符').optional(),
});

/**
 * 费用项数组 Schema
 */
export const salesOrderFeeItemsSchema = z
  .array(salesOrderFeeItemSchema)
  .min(0, '费用项不能为空数组')
  .max(20, '费用项不能超过20个');

/**
 * 自定义验证: 费用总额不能超过订单总额的50%
 */
export const validateFeeTotalAmount = (
  feeItems: z.infer<typeof salesOrderFeeItemsSchema>,
  orderAmount: number
) => {
  const totalFees = feeItems.reduce((sum, item) => sum + item.feeAmount, 0);
  const maxAllowedFees = orderAmount * 0.5;

  if (totalFees > maxAllowedFees) {
    return {
      valid: false,
      message: `费用总额 ${totalFees.toFixed(2)} 元超过订单金额的50% (${maxAllowedFees.toFixed(2)} 元)`,
    };
  }

  return { valid: true };
};

export type SalesOrderFeeItemFormData = z.infer<typeof salesOrderFeeItemSchema>;
```

### 2. Context Provider

```typescript
// components/sales-orders/fee-items/fee-items-context.tsx

import React, { createContext, useContext } from 'react';
import type { UseFieldArrayReturn } from 'react-hook-form';
import type { SalesOrderFeeItemFormData } from '@/lib/validations/sales-order-fee';

interface FeeItemsContextValue {
  fields: UseFieldArrayReturn<any, 'feeItems'>['fields'];
  append: UseFieldArrayReturn<any, 'feeItems'>['append'];
  remove: UseFieldArrayReturn<any, 'feeItems'>['remove'];
  update: UseFieldArrayReturn<any, 'feeItems'>['update'];
  isDisabled: boolean;
}

const FeeItemsContext = createContext<FeeItemsContextValue | null>(null);

export function useFeeItemsContext() {
  const context = useContext(FeeItemsContext);
  if (!context) {
    throw new Error('useFeeItemsContext must be used within FeeItemsProvider');
  }
  return context;
}

export const FeeItemsProvider = FeeItemsContext.Provider;
```

### 3. 主容器组件

```typescript
// components/sales-orders/fee-items/fee-items-form-field.tsx

'use client';

import React from 'react';
import { useFieldArray, type Control } from 'react-hook-form';
import { FeeItemsProvider } from './fee-items-context';
import { FeeItemsHeader } from './fee-items-header';
import { FeeItemsList } from './fee-items-list';
import { FeeItemsSummary } from './fee-items-summary';

interface FeeItemsFormFieldProps {
  control: Control<any>;
  disabled?: boolean;
}

/**
 * 费用项动态表单字段组件
 *
 * 集成 React Hook Form 的 useFieldArray
 * 使用复合组件模式提供清晰的组件结构
 */
export function FeeItemsFormField({
  control,
  disabled = false,
}: FeeItemsFormFieldProps) {
  const { fields, append, remove, update } = useFieldArray({
    control,
    name: 'feeItems',
    // ⚠️ 关键: 使用 field.id 作为 key,而非 index
    keyName: 'key',
  });

  return (
    <FeeItemsProvider
      value={{
        fields,
        append,
        remove,
        update,
        isDisabled: disabled,
      }}
    >
      <div className="space-y-4" role="region" aria-label="费用项管理">
        <FeeItemsHeader />
        <FeeItemsList />
        <FeeItemsSummary />
      </div>
    </FeeItemsProvider>
  );
}
```

### 4. 添加按钮组件

```typescript
// components/sales-orders/fee-items/fee-items-header.tsx

'use client';

import React from 'react';
import { Plus } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Label } from '@/components/ui/label';
import { useFeeItemsContext } from './fee-items-context';
import { getDefaultFeePaidBy } from '@/lib/types/sales-order-fee';

export function FeeItemsHeader() {
  const { fields, append, isDisabled } = useFeeItemsContext();

  const handleAddFeeItem = () => {
    append({
      feeType: 'other',
      feeName: '其他费用',
      feeAmount: 0,
      paidBy: getDefaultFeePaidBy('other'), // 默认承担方
      remarks: '',
    });
  };

  return (
    <div className="flex items-center justify-between">
      <Label className="text-base font-semibold">
        额外费用
        {fields.length > 0 && (
          <span className="ml-2 text-xs font-normal text-muted-foreground">
            (共 {fields.length} 项)
          </span>
        )}
      </Label>
      <Button
        type="button"
        variant="outline"
        size="sm"
        onClick={handleAddFeeItem}
        disabled={isDisabled}
        aria-label="添加费用项"
      >
        <Plus className="mr-2 h-4 w-4" aria-hidden="true" />
        添加费用
      </Button>
    </div>
  );
}
```

### 5. 费用项列表组件

```typescript
// components/sales-orders/fee-items/fee-items-list.tsx

'use client';

import React from 'react';
import { useFeeItemsContext } from './fee-items-context';
import { FeeItemCard } from './fee-item-card';

export function FeeItemsList() {
  const { fields } = useFeeItemsContext();

  if (fields.length === 0) {
    return (
      <div
        className="rounded-lg border border-dashed p-8 text-center"
        role="status"
        aria-live="polite"
      >
        <p className="text-sm text-muted-foreground">
          暂无额外费用，点击上方按钮添加
        </p>
      </div>
    );
  }

  return (
    <div className="space-y-3" role="list" aria-label="费用项列表">
      {fields.map((field, index) => (
        <FeeItemCard
          key={field.key} // ⚠️ 使用 field.key,不是 index
          index={index}
          field={field}
        />
      ))}
    </div>
  );
}
```

### 6. 费用项卡片组件 (核心)

```typescript
// components/sales-orders/fee-items/fee-item-card.tsx

'use client';

import React from 'react';
import { Trash2 } from 'lucide-react';
import { useFormContext } from 'react-hook-form';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select';
import {
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form';
import {
  FEE_TYPE_OPTIONS,
  FEE_PAID_BY_OPTIONS,
  getDefaultFeePaidBy,
} from '@/lib/types/sales-order-fee';
import { useFeeItemsContext } from './fee-items-context';

interface FeeItemCardProps {
  index: number;
  field: any;
}

/**
 * 单个费用项卡片组件
 *
 * 集成 React Hook Form 的 FormField
 * 自动处理验证和错误提示
 */
export function FeeItemCard({ index, field }: FeeItemCardProps) {
  const { control, setValue, watch } = useFormContext();
  const { remove, isDisabled } = useFeeItemsContext();

  // 监听费用类型变化,自动更新承担方
  const feeType = watch(`feeItems.${index}.feeType`);

  React.useEffect(() => {
    if (feeType) {
      const defaultPaidBy = getDefaultFeePaidBy(feeType);
      setValue(`feeItems.${index}.paidBy`, defaultPaidBy);
    }
  }, [feeType, index, setValue]);

  return (
    <div
      className="grid grid-cols-12 gap-3 rounded-lg border bg-card p-4"
      role="listitem"
    >
      {/* 费用类型 */}
      <div className="col-span-3">
        <FormField
          control={control}
          name={`feeItems.${index}.feeType`}
          render={({ field }) => (
            <FormItem>
              <FormLabel htmlFor={field.name}>费用类型</FormLabel>
              <Select
                onValueChange={value => {
                  field.onChange(value);
                }}
                value={field.value}
                disabled={isDisabled}
              >
                <FormControl>
                  <SelectTrigger
                    id={field.name}
                    aria-label="选择费用类型"
                  >
                    <SelectValue placeholder="选择类型" />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  {FEE_TYPE_OPTIONS.map(option => (
                    <SelectItem key={option.value} value={option.value}>
                      {option.label}
                    </SelectItem>
                  ))}
                </SelectContent>
              </Select>
              <FormMessage />
            </FormItem>
          )}
        />
      </div>

      {/* 费用名称 */}
      <div className="col-span-2">
        <FormField
          control={control}
          name={`feeItems.${index}.feeName`}
          render={({ field }) => (
            <FormItem>
              <FormLabel htmlFor={field.name}>费用名称</FormLabel>
              <FormControl>
                <Input
                  {...field}
                  id={field.name}
                  placeholder="如: 国内运费"
                  disabled={isDisabled}
                  aria-label="输入费用名称"
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
      </div>

      {/* 费用金额 */}
      <div className="col-span-2">
        <FormField
          control={control}
          name={`feeItems.${index}.feeAmount`}
          render={({ field }) => (
            <FormItem>
              <FormLabel htmlFor={field.name}>费用金额</FormLabel>
              <FormControl>
                <Input
                  {...field}
                  id={field.name}
                  type="number"
                  step="0.01"
                  min="0"
                  placeholder="0.00"
                  disabled={isDisabled}
                  aria-label="输入费用金额"
                  onChange={e => {
                    const value = parseFloat(e.target.value);
                    field.onChange(isNaN(value) ? 0 : value);
                  }}
                  value={field.value ?? ''}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
      </div>

      {/* ⭐ 承担方 (新增字段) */}
      <div className="col-span-2">
        <FormField
          control={control}
          name={`feeItems.${index}.paidBy`}
          render={({ field }) => (
            <FormItem>
              <FormLabel htmlFor={field.name}>承担方</FormLabel>
              <Select
                onValueChange={field.onChange}
                value={field.value}
                disabled={isDisabled}
              >
                <FormControl>
                  <SelectTrigger
                    id={field.name}
                    aria-label="选择费用承担方"
                  >
                    <SelectValue placeholder="选择承担方" />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  {FEE_PAID_BY_OPTIONS.map(option => (
                    <SelectItem key={option.value} value={option.value}>
                      {option.label}
                    </SelectItem>
                  ))}
                </SelectContent>
              </Select>
              <FormDescription>
                {field.value === 'customer'
                  ? '计入销售收入，不影响成本'
                  : '计入销售成本，影响利润'}
              </FormDescription>
              <FormMessage />
            </FormItem>
          )}
        />
      </div>

      {/* 备注 */}
      <div className="col-span-2">
        <FormField
          control={control}
          name={`feeItems.${index}.remarks`}
          render={({ field }) => (
            <FormItem>
              <FormLabel htmlFor={field.name}>备注</FormLabel>
              <FormControl>
                <Input
                  {...field}
                  id={field.name}
                  placeholder="选填"
                  disabled={isDisabled}
                  aria-label="输入备注"
                  value={field.value ?? ''}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
      </div>

      {/* 删除按钮 */}
      <div className="col-span-1 flex items-end">
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => remove(index)}
          disabled={isDisabled}
          className="text-destructive hover:bg-destructive/10"
          aria-label={`删除第 ${index + 1} 个费用项`}
        >
          <Trash2 className="h-4 w-4" aria-hidden="true" />
        </Button>
      </div>
    </div>
  );
}
```

### 7. 费用汇总组件

```typescript
// components/sales-orders/fee-items/fee-items-summary.tsx

'use client';

import React from 'react';
import { useWatch } from 'react-hook-form';
import { formatCurrency } from '@/lib/utils/format';
import { useFeeItemsContext } from './fee-items-context';

export function FeeItemsSummary() {
  const { fields } = useFeeItemsContext();

  // 使用 useWatch 优化性能,只监听需要的字段
  const feeItems = useWatch({ name: 'feeItems' }) || [];

  if (fields.length === 0) {
    return null;
  }

  const customerPaidTotal = feeItems
    .filter((item: any) => item.paidBy === 'customer')
    .reduce((sum: number, item: any) => sum + (item.feeAmount || 0), 0);

  const companyPaidTotal = feeItems
    .filter((item: any) => item.paidBy === 'company')
    .reduce((sum: number, item: any) => sum + (item.feeAmount || 0), 0);

  const totalFees = customerPaidTotal + companyPaidTotal;

  return (
    <div
      className="space-y-2 rounded-lg border bg-muted/50 p-4"
      role="status"
      aria-live="polite"
      aria-label="费用汇总"
    >
      {/* 客户承担费用 */}
      <div className="flex items-center justify-between">
        <span className="text-sm font-medium text-muted-foreground">
          客户承担费用小计:
        </span>
        <span className="text-sm font-bold text-green-600">
          {formatCurrency(customerPaidTotal)}
        </span>
      </div>

      {/* 公司承担费用 */}
      <div className="flex items-center justify-between">
        <span className="text-sm font-medium text-muted-foreground">
          公司承担费用小计:
        </span>
        <span className="text-sm font-bold text-orange-600">
          {formatCurrency(companyPaidTotal)}
        </span>
      </div>

      {/* 分隔线 */}
      <div className="border-t border-border" />

      {/* 费用总计 */}
      <div className="flex items-center justify-between">
        <span className="text-base font-semibold">额外费用总计:</span>
        <span className="text-lg font-bold text-primary">
          {formatCurrency(totalFees)}
        </span>
      </div>

      {/* 提示信息 */}
      <p className="text-xs text-muted-foreground">
        * 客户承担费用计入销售收入，公司承担费用计入销售成本
      </p>
    </div>
  );
}
```

---

## 🔧 实施步骤

### 第一步: 安装依赖和类型定义 (0.5天)

**1.1 确认依赖版本**

```json
{
  "dependencies": {
    "react-hook-form": "^7.53.2",
    "zod": "^3.24.1",
    "@hookform/resolvers": "^3.9.1"
  }
}
```

**1.2 创建 Zod Schema**

- 创建 `lib/validations/sales-order-fee.ts`
- 定义验证规则和自定义验证函数

**1.3 更新类型定义**

- 确保 `lib/types/sales-order-fee.ts` 包含 `paidBy` 字段

### 第二步: 创建复合组件 (1天)

**2.1 创建组件目录结构**

```
components/sales-orders/fee-items/
├── fee-items-context.tsx      (Context Provider)
├── fee-items-form-field.tsx   (主容器)
├── fee-items-header.tsx        (头部和添加按钮)
├── fee-items-list.tsx          (列表容器)
├── fee-item-card.tsx           (单项卡片) ⭐ 核心
├── fee-items-summary.tsx       (汇总信息)
└── index.ts                    (导出)
```

**2.2 实现各个组件**

- 按照上述代码实现每个组件
- 确保每个组件职责单一
- 添加完整的 TypeScript 类型

**2.3 集成到表单**

```typescript
// components/sales-orders/sales-order-form.tsx

import { FeeItemsFormField } from './fee-items';

<FormField
  control={form.control}
  name="feeItems"
  render={() => (
    <FormItem>
      <FeeItemsFormField
        control={form.control}
        disabled={isSubmitting}
      />
    </FormItem>
  )}
/>
```

### 第三步: 测试和优化 (0.5-1天)

**3.1 单元测试**

```typescript
// components/sales-orders/fee-items/__tests__/fee-item-card.test.tsx

describe('FeeItemCard', () => {
  it('should render all form fields', () => {
    // 测试所有字段正确渲染
  });

  it('should auto-update paidBy when feeType changes', () => {
    // 测试自动更新逻辑
  });

  it('should display validation errors', () => {
    // 测试错误提示
  });
});
```

**3.2 可访问性测试**

- 使用 `axe` 或 `Lighthouse` 进行自动化测试
- 手动测试键盘导航
- 测试屏幕阅读器

**3.3 性能优化**

- 使用 React DevTools Profiler 检查重渲染
- 确认 `useWatch` 正确使用
- 验证 memo 和 useMemo 效果

### 第四步: 迁移现有代码 (0.5天)

**4.1 更新现有表单**

- 将旧的 `FeeItemsInput` 替换为新的 `FeeItemsFormField`
- 更新所有引用

**4.2 数据迁移**

- 确保现有数据兼容新的验证规则
- 测试编辑现有订单

**4.3 清理旧代码**

- 删除或重命名旧的 `fee-items-input.tsx`
- 更新导入路径

**总工作量**: 2.5-3天

---

## 📊 优化效果对比

### 代码质量

| 指标         | 优化前         | 优化后                 | 改善         |
| ------------ | -------------- | ---------------------- | ------------ |
| 代码行数     | 261行 (单文件) | ~180行 (分布在7个文件) | **-31%**     |
| 状态管理代码 | ~80行 (手动)   | ~20行 (useFieldArray)  | **-75%**     |
| 验证代码     | 0行            | ~50行 (Zod Schema)     | **+100%**    |
| 可测试性     | 低 (耦合度高)  | 高 (职责单一)          | **显著提升** |

### 功能完整性

| 功能           | 优化前 | 优化后 |
| -------------- | ------ | ------ |
| 费用类型选择   | ✅     | ✅     |
| 费用名称输入   | ✅     | ✅     |
| 费用金额输入   | ✅     | ✅     |
| 承担方选择     | ❌     | ✅     |
| 备注输入       | ✅     | ✅     |
| 实时验证       | ❌     | ✅     |
| 错误提示       | ❌     | ✅     |
| 键盘导航       | 部分   | ✅     |
| 屏幕阅读器支持 | ❌     | ✅     |
| 自动更新承担方 | ❌     | ✅     |
| 分类汇总显示   | ❌     | ✅     |

### 用户体验

| 体验指标       | 优化前 | 优化后   |
| -------------- | ------ | -------- |
| 实时错误提示   | ❌     | ✅       |
| 自动填充默认值 | 部分   | ✅       |
| 无障碍访问     | 不支持 | 完全支持 |
| 视觉层次清晰度 | 中     | 高       |
| 操作反馈       | 基础   | 完善     |

### 开发体验

| 指标       | 优化前 | 优化后 |
| ---------- | ------ | ------ |
| 类型安全   | 中     | 高     |
| 调试难度   | 高     | 低     |
| 组件复用性 | 低     | 高     |
| 扩展性     | 中     | 高     |
| 维护成本   | 高     | 低     |

---

## ⚠️ 注意事项和最佳实践

### 1. useFieldArray 关键点

```typescript
// ❌ 错误: 使用 index 作为 key
{fields.map((field, index) => (
  <div key={index}>...</div>
))}

// ✅ 正确: 使用 field.id 或 field.key
{fields.map((field, index) => (
  <div key={field.id}>...</div>
))}
```

**原因**: 使用 index 作为 key 会导致表单状态混乱,特别是在删除或重新排序时。

### 2. 受控 vs 非受控

```typescript
// ❌ 错误: 混合使用受控和非受控
<Input value={value} onChange={...} />

// ✅ 正确: 使用 React Hook Form 的 Controller
<FormField
  control={control}
  name="feeItems.0.feeName"
  render={({ field }) => (
    <Input {...field} />
  )}
/>
```

### 3. 验证错误处理

```typescript
// ✅ 自动显示验证错误
<FormField>
  <FormControl>
    <Input />
  </FormControl>
  <FormMessage /> {/* 自动显示错误 */}
</FormField>

// ✅ 手动访问错误
const errors = formState.errors.feeItems?.[index]?.feeName;
if (errors) {
  console.error(errors.message);
}
```

### 4. 性能优化

```typescript
// ✅ 使用 useWatch 优化性能
const feeItems = useWatch({ name: 'feeItems', control });

// 而不是
const feeItems = watch('feeItems'); // 会导致不必要的重渲染
```

### 5. 可访问性清单

- [ ] 所有表单字段都有 `<FormLabel>` 和 `htmlFor` 属性
- [ ] 错误提示通过 `<FormMessage>` 自动关联到字段
- [ ] 使用 `aria-label` 为图标按钮提供描述
- [ ] 使用 `aria-describedby` 关联说明文字
- [ ] 列表使用 `role="list"` 和 `role="listitem"`
- [ ] 动态内容使用 `aria-live="polite"`
- [ ] 所有交互元素支持键盘操作

---

## 🧪 测试策略

### 单元测试

```typescript
// __tests__/fee-item-card.test.tsx

import { render, screen, fireEvent } from '@testing-library/react';
import { useForm, FormProvider } from 'react-hook-form';
import { FeeItemCard } from '../fee-item-card';

describe('FeeItemCard', () => {
  it('should auto-update paidBy when feeType changes to processing', () => {
    const { result } = renderHook(() => useForm());

    render(
      <FormProvider {...result.current}>
        <FeeItemCard index={0} field={{ id: '1', feeType: 'other' }} />
      </FormProvider>
    );

    const feeTypeSelect = screen.getByLabelText('费用类型');
    fireEvent.change(feeTypeSelect, { target: { value: 'processing' } });

    const paidByValue = result.current.getValues('feeItems.0.paidBy');
    expect(paidByValue).toBe('company');
  });
});
```

### 集成测试

```typescript
// __tests__/fee-items-form-field.integration.test.tsx

describe('FeeItemsFormField Integration', () => {
  it('should handle complete fee item lifecycle', async () => {
    const { result } = renderHook(() => useForm());

    render(
      <FormProvider {...result.current}>
        <FeeItemsFormField control={result.current.control} />
      </FormProvider>
    );

    // 1. 添加费用项
    fireEvent.click(screen.getByText('添加费用'));
    expect(screen.getByLabelText('费用类型')).toBeInTheDocument();

    // 2. 填写表单
    // ...

    // 3. 验证汇总
    // ...

    // 4. 删除费用项
    // ...
  });
});
```

### 可访问性测试

```typescript
import { axe, toHaveNoViolations } from 'jest-axe';

expect.extend(toHaveNoViolations);

it('should have no accessibility violations', async () => {
  const { container } = render(<FeeItemsFormField {...props} />);
  const results = await axe(container);
  expect(results).toHaveNoViolations();
});
```

---

## 📚 参考资源

### 官方文档

- [React Hook Form Documentation](https://react-hook-form.com/)
- [Zod Documentation](https://zod.dev/)
- [shadcn/ui Form Component](https://ui.shadcn.com/docs/components/form)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)

### 最佳实践文章

- [Building Advanced React Forms Using React Hook Form, Zod and Shadcn (2025)](https://wasp.sh/blog/2025/01/22/advanced-react-hook-form-zod-shadcn)
- [React Design Patterns: Compound Component Pattern](https://www.patterns.dev/react/compound-pattern/)
- [React in 2025: The Essential Developer's Guide](https://frontdose.com/posts/react-essential-guide-2025/)

---

## ✅ 验收清单

### 功能完整性

- [ ] 所有费用项字段正常显示和编辑
- [ ] "承担方"字段正确添加并工作
- [ ] 费用类型变更时自动更新承担方
- [ ] 添加/删除费用项功能正常
- [ ] 费用汇总显示正确(客户/公司分类)

### 代码质量

- [ ] 使用 React Hook Form + useFieldArray
- [ ] 使用 Zod Schema 验证
- [ ] 使用复合组件模式
- [ ] 所有组件有完整 TypeScript 类型
- [ ] 通过 ESLint 和 Prettier 检查

### 验证和错误处理

- [ ] 所有字段实时验证
- [ ] 错误提示友好且准确
- [ ] 自定义验证规则生效
- [ ] 表单提交时验证拦截

### 可访问性

- [ ] 通过 axe 或 Lighthouse 测试
- [ ] 所有表单字段有正确的标签
- [ ] 支持键盘导航(Tab, Enter, Esc)
- [ ] 支持屏幕阅读器
- [ ] 错误提示可被辅助技术识别

### 性能

- [ ] 无不必要的重渲染
- [ ] useWatch 优化生效
- [ ] React DevTools Profiler 检查通过

### 测试覆盖

- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试覆盖关键流程
- [ ] 可访问性测试通过

### 文档

- [ ] 代码注释完整
- [ ] 组件 JSDoc 完整
- [ ] 使用示例清晰

---

**文档完成日期**: 2025-01-11
**下一步**: 开始实施第一步 (安装依赖和类型定义)
