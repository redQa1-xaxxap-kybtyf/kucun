const config = {
  // 开发时可用本机地址；上线前改成 ERP 的 HTTPS 域名，并加入小程序 request 合法域名。
  // 微信开发者工具里 127.0.0.1 比 localhost 更稳定；真机预览要改成电脑局域网 IP 或线上域名。
  apiBaseUrl: 'https://kucun.0595t.com',
};

module.exports = config;
