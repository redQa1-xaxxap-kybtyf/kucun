/* 库存查询页样式 - Apple Style */

.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background-color: var(--ios-bg);
}

/* 搜索区域 */
.search-section {
  background-color: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  padding: 8rpx 0;
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 10;
}

/* 筛选区域 */
.filter-section {
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
}

/* 滚动容器 */
.scroll-container {
  flex: 1;
  height: 100%;
}

/* 库存列表 */
.inventory-list {
  padding: 24rpx 32rpx;
}

.inventory-item {
  background-color: var(--ios-card-bg);
  border-radius: var(--ios-radius);
  margin-bottom: 24rpx;
  overflow: hidden;
  box-shadow: var(--ios-shadow);
}

/* 项目头部 */
.item-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 32rpx;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.05); /* Lighter separator */
  background-color: rgba(242, 242, 247, 0.5); /* Subtle contrast */
}

.product-name {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--ios-text-primary);
  flex: 1;
  margin-right: 16rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Vant overrides */
.van-cell {
  background-color: transparent !important;
  padding: 24rpx 32rpx !important;
}

.van-cell::after {
  left: 32rpx !important;
  right: 32rpx !important;
  border-bottom-color: rgba(0, 0, 0, 0.05) !important;
}

/* 时间标签 */
.time-label {
  font-size: 22rpx !important;
  color: var(--ios-text-tertiary) !important;
}
